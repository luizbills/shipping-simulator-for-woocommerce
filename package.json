{
    "name": "wc-shipping-simulator",
    "version": "1.0.0",
    "scripts": {
        "start": "./scripts/bundle --watch",
        "build": "./scripts/bundle",
        "postinstall": "npm run build"
    },
    "devDependencies": {
        "cli-color": "^2.0.3",
        "esbuild": "^0.14.49"
    }
}
